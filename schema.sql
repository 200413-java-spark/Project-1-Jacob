create table sparkdb(
    course_title varchar,
    is_paid Boolean,
    price integer,
    num_subscribers integer,
    num_lectures integer,
    level varchar,
    content_duration varchar,
    subject varchar
);